# Finplot 项目协作规则

## 开始工作

先阅读 README.md、docs/PROJECT-MEMORY.md、docs/DATABASE.md、docs/ROADMAP.md 和 design/DESIGN-NOTES.md。设计基准为 design/index.html 与 design/previews/，原始可编辑片段在 design/folio-finance.fragment.html。

## 用户意图与授权

用户已决定由当前助手主导 Finplot，沿用已认可的设计地基，先与真实数据库匹配，再按后续需求逐步成长。本次准备工作仅建立项目地基；尚未完成真实数据接入，也没有确定正式技术栈。不要因旧团队文案曾列出九个模块，就将其当作已确认的全部产品规格。

用户重视美感和可读性，不接受功能堆砌、厚重阴影和后台式统一模板。保留分析能力，同时围绕用户问题重组层级。无需对普通可逆实现反复请求确认；涉及未确定的数据口径时先查证，再明确问题。

## 数据与安全

data/ 内为私人账单，禁止提交到 Git 或打包进公开前端。原始 iCloud 备份路径记录在 docs/DATABASE.md，只读访问，禁止修改原文件。工作副本也默认只读。schema.sql 可用于理解结构；金额聚合使用 amount_cents 整数，最后格式化为元，避免浮点误差。

导入来源不等于支付账户。数据库没有账户、持仓、行情表，不得虚构账户余额、未实现收益或收益率。nature 与 direction 是不同维度；往来、调整的排除口径尚待确认。标签是一对多连接，汇总时防止重复计数。时间字符串的时区约定需确认。

## 设计实施

先匹配真实数据，再扩展；不把示例交易、示例分类和示例周期逻辑当成生产逻辑。保留用户确认的视觉方向，品牌正式名称为 Finplot，“澄明 Folio”为设计原型名。按实际页面任务决定指标数量和布局。适配 1440px 与 390px，避免整页横向滚动；验证长文本、负数、无记录和键盘焦点。

## 持续记忆与提交

完成重要决策后更新 docs/PROJECT-MEMORY.md 与 docs/ROADMAP.md，记录已完成、未完成和待确认口径。所有 git commit message 必须使用简体中文，例如 feat: 接入月度收支汇总。

## English Coaching

用户用英文表达且有明显语法或措辞错误时，在回复末尾按每行“😇 原文 → 修正 (Pattern name)”提供简短纠正。中文消息不需要纠正；引用的他人文案不作为用户语言错误。
