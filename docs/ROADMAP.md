# 实施路线

1. [已完成首轮] 建立只读数据访问层：读取 SQLite，统一金额、日期、方向和性质格式；金额聚合使用整数分。
2. 将总览页示例数据替换为真实月度汇总与最近交易。
3. 处理真实数据状态：无记录、加载中、错误、无匹配、长文本和极端金额。
4. 完善收支分析：周期、方向、分类组、性质、标签和来源筛选，支持从图表进入交易明细。
5. 明确投资口径后实现投资页，不先假定浮动收益计算方式。
6. [已完成收支日历首版] 扩展周期对比、日常财务、长期趋势和全部交易页。
7. 进行 1440px / 390px 视觉验收、键盘操作验收和数据库边界验收。

## 真实页面实施更新

已沿用设计原型样式实现总览、收支分析、分类交易展开、全部交易和投资记录。月份/年度、性质、方向筛选统一作用于指标和明细；趋势展示所选年度。默认日常可切换全部或单一性质，未将默认值视作已确认业务口径。投资页不提供虚构持仓与收益率。

服务入口 `python3 app/server.py`，端口 8765；仅允许公开 index.html、app.js 与只读 API，禁止目录浏览。真实记录由本地 API 动态读取，前端以整数分聚合；没有静态打包私人数据。当前为本地原型，正式服务与部署技术栈尚待确定。

验证：1440px/390px 页面导航、无横向溢出、年度切换、收入分类、分类展开、隐私隐藏及空数据通过浏览器自动化检查。桌面截图已检查。服务重启后本机及 Tailscale 地址可访问。尚未配置开机自启，主机休眠时无法保证远程访问；时间时区、投资会计口径仍待确认。

## 启动与跨机器使用

已增加 macOS 双击入口 `启动Finplot.command`；数据库改为环境变量或 `data/` 唯一 SQLite 文件自动发现，多文件时要求显式指定。

数据库路径体验已补齐：macOS 启动文件支持首次系统文件选择并持久化路径，跨机器 clone 后无需终端命令或复制账单文件。


## v0.1.1 收支颜色与图例

已统一收入（暖红色 #b34436）和支出（绿色 #24734f）的金额与图表语义色；差额为盈余、超支或中性平衡，空记录单独标记。保留方向文字与金额正负号，趋势图及分类图使用实心／斜纹图例，不再用“深蓝／浅蓝”句子解释。隐私模式隐藏差额状态和语义色。

参考 Apple HIG Charts 和 Color 的包容性颜色建议；采用颜色之外的图案编码及趋势读屏摘要。保持已认可的布局、蓝色交互强调和真实数据口径，设计原型保留作基准。验证覆盖 1440px / 390px 四个页面无横向溢出、盈余／超支／平衡／无记录、隐私隐藏和浏览器运行错误检查；金额语义色对白底对比度超过 4.5:1。业务口径与时区仍待确认。

用户确认采用中文财务习惯：收入／盈余为红色，支出／超支为绿色；实心／斜纹和文字状态维持不变。

界面精简：按用户要求移除指标卡底部解释、隐藏金额按钮及全部隐藏逻辑、侧栏左下角账本与币种描述；金额和图表始终显示。此前隐私模式记录为历史状态，不再适用于当前版本。

界面精简：移除“统计性质”筛选。总览、收支分析与全部交易保留原默认日常口径，投资页固定投资口径；本次仅移除控件，不将统计范围改为全部性质。

## v0.1.2 交易分类图标

已为交易表每一行增加分类符号图标，覆盖启用中的生活、出行、家庭、休闲、学习、购物、旅行、人情、投资、往来、工作收入、经营与退款等类型；旧记录通过分类组或通用符号回退。图标仅用于识别，不参与数据口径。

## 时间选择器下一步

已确认紧凑的月度／年度切换、左右周期翻页及点击日期打开选择面板。日期旁不显示下拉箭头；实现时保留明确的悬停、键盘焦点和触控点击区域。概念图已更新，实际页面待接入。

## 时间选择器完成

真实页面已接入确认的设计：月度／年度、左右翻页、无下拉箭头的中文日期入口、月／年选择面板、回到本月／今年及键盘操作。已完成跨年与周期状态验证、真实金额核对、1440px／390px 四页面布局和弹窗视觉检查。

时间选择器边界已完成：以全账本首末交易周期限制箭头与弹窗选择，到界变灰且禁用；当前周期快捷入口同样遵守边界。已验证真实边界、单月／空账本和键盘导航，并修复弹窗翻年后意外关闭。

时间选择器悬停问题已修复并验证：选中态不再被灰色 hover 覆盖，回到本月／今年按钮背景圆角且文字居中。

## 2026-09-23 收支日历

已完成收支日历首版：每日收支聚合、日期选择、当天交易明细、账本范围外提示、桌面并排与移动纵向布局，以及键盘日期导航。

## 2026-09-23 图表调整

已完成投资累计收益曲线、收支差额柱形、日期单位标签、图表悬停浮层、总览累计结余小图，以及收支分析收入／支出并列模块。后续仍需继续做不同视口下的视觉验收与更细的无障碍键盘交互。

图表交互修订：月份整格悬停、延迟显示节点与参考线、投资柱形统一正向表达、结余副图改为对齐的柱状图，以及分析页移除方向切换器已完成。

图表零值、投资盈利颜色与总览副图横坐标对齐问题已修订并验证。

## 2026-09-23 数据范围筛选

已确认后续实现统一的数据范围按钮：总览、收支分析、收支日历等非投资、非全部交易页面均提供悬浮范围选项框，选项固定为全部、日常、投资、往来、调整五项；全部与其余四项互斥，其余四项支持多选自定义组合。范围需同时作用于指标、图表、分类、日历与明细，并通过按钮文案标明当前口径。

全部交易页默认展示完整记录，移除收入、支出、收支差额三个统计框；投资页固定投资范围，不增加通用范围按钮。实现时需补充范围元信息、跨模块一致性测试，以及 1440px / 390px 下悬浮选项框的键盘与触控验收。

范围交互补充：全部选项下方使用分隔线，其余四项直接多选，不设置“自定义”入口或标签；按钮展示全部或具体已选性质。每个页面独立记住自己的范围，返回该页恢复选择。以上设计已实现，详见下方验收记录。


统计范围已完成：三个页面的独立持久化范围、全部互斥与四项多选、明确空选择、统一指标与图表口径、全部交易完整记录及去统计框。自动化验收覆盖全组合金额核对、各页记忆、全年趋势、年度选择、分类钻取、空账本、桌面与移动布局及键盘交互；1440px／390px 截图已目视检查。没有增加额外统计性质预设或“自定义”入口。

## 2026-09-23 分类明细修复

已修复收支分析收入构成条形点击后明细为空：钻取方向改由实际点击的分类按钮传递，与总览方向状态分离。新增收入／支出分类钻取回归测试并通过，既有范围测试通过。


## v0.1.5 版本整理

用户指定发布 v0.1.5，README 与 CHANGELOG 已更新。版本范围包含自 v0.1.4 后的收支日历、独立页面统计范围、全部交易完整记录与精简，以及收入分类钻取修复。私人账单、真实金额截图和未确认的月度分组设计预览不纳入本次提交。

## 2026-09-23 总览图表选月与移动导航完成

已完成总览主／副图选月、重复点击回年度、与日期选择器及总览数据同步、月度常驻虚线和年度默认隐藏虚线。桌面保留不改变周期的悬浮预览，移开恢复选中态；触屏保留点击选月与纵向滚动。图表月份遵守账本日期边界，并支持键盘选择与取消。

移动顶栏已收为品牌与导航同一行，超出入口横向滚动，不再向下堆叠；键盘焦点自动显示对应入口。合成数据回归覆盖 1440px／390px／320px，含空数据、负结余、跨年与统计范围保持；原有范围和分类明细回归通过，截图已检查。未扩展其他页面图表的选月行为，未改变既有业务口径待确认项。

总览图表交互细化已完成：悬浮主图或副图时双图虚线实时同步，离开统一恢复；月份粗蓝焦点框替换为不随 SVG 缩放的 1px 淡蓝键盘焦点描边。已补充自动化测试并检查视觉截图。

投资图表现已复用总览的月份选择／重复点击回年度、常驻虚线、悬浮预览恢复及轻量焦点样式。投资统计范围和累计计算不变，已通过三种宽度下的投资页交互与数据联动回归。

## 2026-09-23 投资页术语独立化完成

投资页已完成专业术语调整：摘要卡、页面标题、趋势图标题与图例、悬浮预览、记录区标题及空状态均使用投资语境的“盈利／亏损／投资盈亏／累计盈亏”。底层统计逻辑与投资固定范围不变，现有三组回归测试通过。


## 2026-09-23 产品壳层

- 已完成：产品名下版本号、侧边栏固定、设置入口和更新日志子页面。
- 后续设置项尚未确认，本次只提供更新日志，不预设额外功能。

验收：新增设置与更新日志回归，覆盖 1440px／390px／320px 的版本号上下布局、侧边栏内设置入口、子页面返回、滚动时导航固定、无横向溢出与键盘操作；核对日志与 CHANGELOG 一致。既有分类明细、图表选月及范围回归全部通过，桌面与移动滚动截图已检查。测试使用合成或空账本，无私人数据提交。

位置修正：用户明确设置入口应位于左下角的侧边栏内部，而非页面右下角。已移动到侧栏底部；手机端随侧栏布局放在顶部导航内。设置与图表回归在 1440px／390px／320px 下通过。

## 2026-09-24 版本检查与更新完成（未发布）

- 设置页：主动检查稳定 tag、版本比较、确认更新、进度轮询、重启后恢复与失败提示。
- 运行：version.json 单一版本源；启动器切换至 supervisor；直接 server.py 保留只读检查能力。
- 安全：HTTPS 获取固定 tag 提交、仅 fast-forward、本地修改／冲突阻断、不碰数据库、健康验证和条件回退、重复更新保护。
- 验收：独立临时 Git 仓库及合成账本后端测试、真实重启／退出、桌面与手机 UI 及既有回归通过。
- 待办：用户确认正式版本后同步发布资料并发布 tag；账户认证与 launchd 继续暂缓。断电等外部中断可能需要人工检查，不承诺自动恢复所有故障。

## v0.1.8 版本整理

2026-09-24 用户确认发布 v0.1.8 并推送 Git tag。版本文件、README、CHANGELOG 与前端日志已同步，发布范围为版本检查、安全更新和 supervisor 重启。此前“未发布”记录为实现阶段状态；私人数据库及未确认的 output 预览不纳入提交。账户认证与 launchd 仍暂缓，业务口径不变。
