# Finplot 项目记忆

## 产品目标

打造一个让个人能快速看懂自己财务状态、收支结构、结余趋势和投资表现的 Web 应用。它应当像一个清晰、可信、安静的个人工具，而不是工程后台或传统报表系统。

## 已确认的设计方向

- 品牌方向：澄明 Folio；克制、清晰、可信、有 Apple 产品的秩序感。
- 信息顺序：状态 → 变化 → 结构 → 明细。
- 首页默认突出结余、收入、支出和收支趋势；分析页再进入类别结构；投资页单独处理收益与持仓。
- 使用浅灰背景、白色表面、柔和分隔线和蓝色交互强调；颜色只承担语义，不用颜色单独传达收入或支出。
- 通过排版、比例和留白建立层级，避免厚重阴影、深色卡片墙、重复的大标题和无意义的巨大空白。
- 图表必须支持判断：有清晰的时间、数量、单位和比较关系；不使用装饰性假图。
- 桌面端使用并排关系帮助比较，窄屏端恢复顺序阅读，不产生整页横向滚动。

## 对旧方案的纠偏

九个功能模块不应在首页被做成九张同等重量的卡片；通用模板不能覆盖页面目标；筛选不应占据大面积却只提供几个控件；支出页不应重复展示无关的收入指标；图表不能用密集小卡补救不可读的尺度。

## 数据口径注意事项

- 备份中的交易金额是正数，方向字段决定收入或支出。
- `nature` 可以区分日常、投资、往来和调整；分析页需要明确是否排除往来与调整。
- 投资页必须先确认产品要展示已实现收益、浮动收益、持仓市值，还是三者分开；设计原型中的投资持仓数字是示例，不是已确认业务口径。
- 类别、分类组和别名均由数据库提供；前端不应复制一份静态分类规则作为唯一来源。
- `source_record_id` 用于去重和追溯来源，相关交易页应保留来源信息筛选能力。

## 工作方式

每次扩展先明确页面要回答的问题和数据口径，再决定指标、图表和明细。实现后在 1440px 和 390px 下对照设计稿，检查比例、文字换行、长数字、空数据、无匹配、错误和键盘焦点。不要因为功能增加而恢复成密集后台布局。

## 2026-09-22 工作区交接状态

用户正式指定工作区 /Users/x/Desktop/Finplot。已迁入离线交互设计、原始源码、8 张高清预览图、设计说明、数据库副本、元数据与月度汇总导出、完整 SQL 结构及本项目规则。正式产品名称使用 Finplot。

正式应用尚未开始实现，app/ 为预留空目录；没有选定框架、后台或部署方式；没有接入真实数据。下一次从只读查询与指标映射开始，先总览与收支结构，不默认扩展全部旧模块。数据库无持仓和账户信息，投资原型仅用于视觉参考。原型隐藏金额仍保留图表刻度，仅作交互演示。

## 2026-09-22 首轮真实数据匹配

- `app/ledger.py` 已建立只读 SQLite 访问层，使用 `mode=ro` 打开工作副本。
- 汇总函数返回整数分：2026-09 月全口径收入 6,624.33 元、支出 13,013.50 元、结余 -6,389.17 元；仅日常性质时支出 12,318.61 元、结余 -5,694.28 元。
- 数据库实际范围为 2024-07-18 11:01:00 至 2026-09-21 20:16:56，共 1,667 笔交易。
- 设计稿中的 2026 年 9 月收入 18,500 元、支出 9,860.73 元与真实数据不符，不能继续直接使用。
- “总览”默认是否排除往来、调整，以及投资性质是否单列，仍待业务确认；访问层已支持通过 `natures` 显式传入口径。

## 真实页面实施更新

已沿用设计原型样式实现总览、收支分析、分类交易展开、全部交易和投资记录。月份/年度、性质、方向筛选统一作用于指标和明细；趋势展示所选年度。默认日常可切换全部或单一性质，未将默认值视作已确认业务口径。投资页不提供虚构持仓与收益率。

服务入口 `python3 app/server.py`，端口 8765；仅允许公开 index.html、app.js 与只读 API，禁止目录浏览。真实记录由本地 API 动态读取，前端以整数分聚合；没有静态打包私人数据。当前为本地原型，正式服务与部署技术栈尚待确定。

验证：1440px/390px 页面导航、无横向溢出、年度切换、收入分类、分类展开、隐私隐藏及空数据通过浏览器自动化检查。桌面截图已检查。服务重启后本机及 Tailscale 地址可访问。尚未配置开机自启，主机休眠时无法保证远程访问；时间时区、投资会计口径仍待确认。

数据库定位与启动：`app/ledger.py` 优先读取显式参数和 `FINPLOT_DATABASE`，否则自动发现 `data/` 中唯一的 SQLite 文件；发现多个或没有文件时明确报错，不静默猜测。根目录 `启动Finplot.command` 可在 macOS 双击启动服务并打开浏览器。数据库文件仍不会进入 Git。

数据库路径体验更新：macOS 启动文件首次找不到数据库时调用系统文件选择器，选中的路径保存到被 Git 忽略的 `.finplot-database`；后续双击自动复用。网页不直接承担本机任意路径选择，避免浏览器安全限制。


## v0.1.1 收支颜色与图例

已统一收入（暖红色 #b34436）和支出（绿色 #24734f）的金额与图表语义色；差额为盈余、超支或中性平衡，空记录单独标记。保留方向文字与金额正负号，趋势图及分类图使用实心／斜纹图例，不再用“深蓝／浅蓝”句子解释。隐私模式隐藏差额状态和语义色。

参考 Apple HIG Charts 和 Color 的包容性颜色建议；采用颜色之外的图案编码及趋势读屏摘要。保持已认可的布局、蓝色交互强调和真实数据口径，设计原型保留作基准。验证覆盖 1440px / 390px 四个页面无横向溢出、盈余／超支／平衡／无记录、隐私隐藏和浏览器运行错误检查；金额语义色对白底对比度超过 4.5:1。业务口径与时区仍待确认。

用户确认采用中文财务习惯：收入／盈余为红色，支出／超支为绿色；实心／斜纹和文字状态维持不变。

界面精简：按用户要求移除指标卡底部解释、隐藏金额按钮及全部隐藏逻辑、侧栏左下角账本与币种描述；金额和图表始终显示。此前隐私模式记录为历史状态，不再适用于当前版本。

界面精简：移除“统计性质”筛选。总览、收支分析与全部交易保留原默认日常口径，投资页固定投资口径；本次仅移除控件，不将统计范围改为全部性质。

## v0.1.2 交易分类图标

- 交易明细行左侧按数据库返回的明细分类显示轻量符号图标，当前启用类型均有明确映射。
- 历史停用类型或未来未知分类按分类组回退，再使用通用符号；不改变数据库分类与汇总口径。

## v0.1.2 图标统一修订

交易分类图标采用同一套线性 SVG，统一描边与尺寸；分类仍由数据库名称映射，未知类型继续按分类组回退。

## v0.1.2 分类色彩

交易图标沿用方向与行为大类语义：支出采用冷色系，收入采用暖色系，同一大类共享色相；图标仍保持线性 SVG 的统一视觉规范。

## 时间选择器设计确认

用户认可 `output/design/period-picker-concept.png` 中月度／年度切换、左右翻页和点击日期展开选择面板的方案。进一步确认去掉日期右侧下拉箭头，保留左右周期切换箭头；日期整块作为点击入口。当前已更新概念图，实际页面尚待实现。

## 时间选择器实现完成

已在真实页面替换原生月份输入框：月度／年度分段切换、左右周期翻页、中文日期整块点击展开，无日期下拉箭头。月度面板为 12 个月，年度面板为 12 年并支持翻页；回到本月／今年按本地当前日期计算。月／年互切保留月份，页面导航保留所选周期；初次加载沿用最新账单月份。趋势标题明确展示所选年份。

支持点击外部关闭、Escape 关闭并返回日期按钮、Tab 离开关闭，以及面板方向键和 Home／End 导航。已通过真实浏览器检查跨年、选年、月份记忆、返回当前周期、页面状态保持、空记录、真实金额汇总与 1440px／390px 四页面无溢出，桌面和移动截图已检查。数据库与统计口径不变。

## 时间选择器交易日期边界

按用户要求，时间导航以整个账本第一笔至最新一笔交易所在周期为界，与页面性质筛选无关。到达月／年边界时箭头变灰并原生 disabled；弹窗的越界月份、年份、翻页也不可选，范围内无交易的月份仍可查看。本月／今年不在账本范围内时快捷入口禁用；无记录时日期入口和翻页均禁用。年度切回月度时，若记住的月份越界则收敛到最近有效月份。同步修复弹窗翻年重绘按钮被误判为点击外部导致关闭的问题。

验证通过真实账本月／年上下边界、弹窗限制、键盘跳过禁用项、年度切回月份边界、390px 布局，以及模拟单月历史账本和空账本。

## 时间选择器悬停修复

通用灰色 hover 背景仅作用于未选中且可用的按钮，月度／年度选中项保持白底、已选日期保持蓝底。回到本月／今年的分隔线移至独立页脚，按钮采用对称内边距、居中布局和圆角，避免悬停背景与文字偏移。真实浏览器已验证月／年两种模式的选中背景、按钮圆角与文字中心位置，年度悬停截图已检查。

## v0.1.3 版本整理

用户确认发布 v0.1.3；README 与 CHANGELOG 已同步版本，范围为时间选择器设计、实现、交易日期边界及悬停修复。私人账单和包含真实金额的验收截图不进入版本提交。

## 2026-09-23 图表交互与结构调整

- 投资记录趋势改为投资性质下按月收支差额累积的平滑曲线，保留差额柱形；移除投资页口径说明文案。
- 日期横轴标签统一显示中文单位（如“1月”“9月”），图表数据点支持悬停浮层。
- 财务总览趋势下增加复用月份横坐标的累计结余小图。
- 收支分析移除“本期观察”，收入构成与支出构成拆为两个并列模块。

## 2026-09-23 图表交互修订

- 图表悬停区域扩大为每个月份的完整横向网格；默认不显示投资曲线节点，悬停后显示节点与虚线。
- 投资柱形使用统一绿色正向金额表达，不再以负高度表示亏损；累计收益曲线减细并增加渐变阴影。
- 总览副图改为按月结余柱形图，与主图使用相同月份坐标；分析页收入／支出模块不再显示方向切换器。

## 2026-09-23 图表零值与颜色修订

- 零值柱形不再使用最小高度，完全隐藏为 0 高度。
- 投资月度差额柱形按正负语义显示：盈利红色、亏损绿色斜纹；零值不绘制可见柱形。
- 总览结余副图移除固定高度造成的横向缩进，改为与主图相同的 600 视图宽度和响应比例。

## 2026-09-23 收支日历页面

- 新增“收支日历”页面，沿用日常性质口径和既有月份边界，按账本记录的原始日期聚合每日收入与支出。
- 月历日期格显示收入／支出金额，收入使用暖红色、支出使用绿色；超过账本首末日期的日期明确标记为账单范围外。
- 桌面端采用月历与右侧当天明细并排布局，移动端上下排列；点击日期和方向键均可移动选择，明细复用分类图标与交易样式。
- 暂未进行时区转换；数据库时间没有显式时区，仍待业务确认。计划数据和账单提醒不属于本次范围。

- 收支日历固定月度视图；年度切换保留给其他页面，避免在日历页重复展示 12 个月的日期格。

## 2026-09-23 数据范围筛选决策

用户确认：除投资页与全部交易页外，总览、收支分析、收支日历等页面新增统一的数据范围按钮。点击后以悬浮选项框切换统计范围，选项固定为“全部”“日常”“投资”“往来”“调整”五项；“全部”与其余四项互斥，其余四项支持多选自定义组合。

- 范围按钮是轻量上下文入口，不恢复厚重的后台式筛选区；按钮文案应反映当前范围，例如“范围：日常”。
- “全部”表示四种 `nature` 全部包含；“往来”和“调整”需要保持可单独选择，不能只合并为一个不可拆分的选项。
- 悬浮框在“全部”下设置分隔线，其下依次为日常、投资、往来、调整四个多选项；不设置“自定义”选项或“范围：自定义”文案。选择全部时仅全部选中；选择具体性质时退出全部状态，按需添加或取消具体性质。
- 按钮显示“范围：全部”或具体已选性质，不使用“自定义”替代范围信息。
- 各页面独立记住自己的统计范围，切换页面后返回恢复该页的选择，不能相互覆盖。实现采用本浏览器 localStorage 保存各页范围，刷新或重开页面可恢复；不存储账单记录。
- 统计卡片、图表、分类汇总、日历和明细必须使用同一范围，不能只改变其中一部分。
- 全部交易页默认展示完整记录，不再展示收入、支出、收支差额三个统计框；本轮不增加交易页筛选控件。
- 投资页继续固定投资范围，不显示通用范围按钮。
- 范围标签必须提示当前口径；包含投资、往来或调整时，不应继续使用容易被理解为“生活消费”的标题。


### 统计范围实现与验收

- 总览、收支分析、收支日历已接入同一套范围筛选，默认为日常，各页独立持久化记忆；投资固定投资，全部交易包含所选日期周期内的所有性质。
- 弹层严格采用“全部／分隔线／日常、投资、往来、调整”结构，无“自定义”选项或标签。全部与四项互斥，选择具体性质退出全部状态，多选不关闭面板。全部取消时显示“未选择性质”和零记录，不自动扩大到全部。四项全勾仍保留具体勾选状态。
- 同一范围应用于当前周期指标、分类、最近交易、日历与当天明细，以及全年收支主图和结余副图；日期导航仍按整个账本边界。
- 全部交易页已移除三个统计框；投资与全部交易均不显示范围按钮。
- 支持 Esc 关闭并回到按钮、点击外部关闭、焦点离开关闭，日期和范围弹层互斥。存储不可用时回退为当前会话记忆。
- 新增 `tests/scope.cjs`：验证全部与 16 种具体性质组合、三个页面的整数金额和笔数、总览全年图表、跨页与刷新记忆、完整交易、空账本，以及 1440px／390px 无横向溢出和弹层键盘交互。截图仅保存在临时目录，未提交私人账单或真实金额截图。

## 2026-09-23 收入分类明细修复

收支分析并列显示收入／支出构成后，点击收入条形仍沿用总览的默认支出方向，导致明细被错误过滤为空。现由分类按钮携带实际方向，单独记录分类明细方向，不再依赖或修改总览方向。统计范围与日期过滤保持不变。

新增 `tests/category-details.cjs`，以合成记录先复现收入分类应有 2 笔但实际为 0 的错误，再验证修复。覆盖收入／支出交替点击、月／年、全部／日常范围、明细金额及收起、总览方向独立、键盘操作和 1440px／390px；原范围筛选回归测试也全部通过。


## v0.1.5 版本整理

用户指定发布 v0.1.5，README 与 CHANGELOG 已更新。版本范围包含自 v0.1.4 后的收支日历、独立页面统计范围、全部交易完整记录与精简，以及收入分类钻取修复。私人账单、真实金额截图和未确认的月度分组设计预览不纳入本次提交。

## 2026-09-23 总览图表选月与移动单行导航

- 总览收支主图、结余副图与日期选择器共用周期／月份状态：月度时显示已选月份虚线；年度时不显示常驻虚线。点击任一图表中的月份切换到该月，再次点击当前月份取消选择并回到该年总览。指标、构成、最近交易同步切换，旧分类明细收起，统计范围保持不变；图表继续展示该年 12 个月。
- 桌面鼠标悬浮保留金额预览，只临时移动参考线，不改变页面统计周期；移开恢复选中月份，年度模式则隐藏参考线。触屏点击不触发悬浮预览，纵向滑动图表仍可滚动页面。
- 月份点击边界与日期选择器一致，使用整个账本的首末月份；边界内零记录月份可选，边界外月份和空账本不可选，不自动跳到其他月份。月份支持 Tab 聚焦、Enter／空格选择和取消，具有可见焦点与选中／禁用语义。投资图保留原有悬浮交互，不参与选月。
- 手机端品牌与主导航同处一行，导航不换行，超出部分横向滚动；全部入口仍可访问，键盘聚焦时自动滚入可见区域。
- 新增 `tests/chart-selection.cjs`，使用合成账单验证 1440px／390px／320px 下主副图点击、触控、月年切换、跨年、空月／空账本／边界、金额与笔数、日期面板、统计范围保持、分类明细清理、悬浮恢复及键盘焦点。原有范围与分类明细测试通过；三种宽度截图已目视检查，无整页横向滚动。截图在临时目录，不提交私人数据。
- 本次不变更数据库或业务口径，时区约定等既有待确认事项保持不变。

### 总览虚线同步与焦点样式修订

- 主图／副图的悬浮月份实时同步：悬浮任一图时，两条参考线均移动到同一月份，仅当前图显示金额浮层；离开后两图一起恢复选中月份，年度模式一起隐藏。不改变当前统计周期。
- 移除月份 SVG 的默认粗焦点外框；需要显示键盘焦点时，使用 1px 淡蓝描边，设置 non-scaling-stroke 防止随 SVG 尺寸变粗。点击选中仍由虚线表达。
- 补充双向悬浮同步、离开恢复和焦点样式断言；1440px／390px／320px 图表交互回归通过，桌面同步预览与细焦点截图已检查。

### 投资图表复用月份交互

按用户后续要求，将此前仅用于总览的选月行为应用到投资图表：月度常驻当前月份虚线，点击其他月份同步更新日期、指标与投资交易，再次点击已选月份返回年度。桌面悬浮仍只预览累计收益与当月差额，移开恢复当前选择；触控与键盘共用月份目标和 1px 非缩放焦点描边。

投资固定 nature=投资、全年曲线及累计计算方式均不变，月份边界沿用日期选择器的全账本范围，边界内没有投资记录的月份显示零记录。新增投资页回归验证月年切换、金额／明细、全年图表不受选月截断、悬浮节点恢复、键盘、触控、日期选择与越界保护；1440px／390px／320px 测试通过。

### 2026-09-23 投资页术语独立化

投资页不再复用总览页的“收支差额／收入／支出”摘要文案，改为投资语境：摘要卡使用“投资盈亏／盈利／亏损”，状态使用“盈利／亏损／盈亏平衡”；页面标题改为“看清投资盈亏”。趋势图标题改为“投资盈亏趋势”，图例使用“盈利／亏损”，悬浮信息使用“累计盈亏／当月盈亏”。交易明细区改为“投资记录”，投资空状态改为“暂无投资记录”。底层方向和金额计算仍沿用收入／支出字段，不改变数据库口径。


## 2026-09-23 设置与固定侧栏

- 产品名称下方显示当前版本号 `v0.1.7`，版本与 `README.md`、`CHANGELOG.md` 保持一致。
- 侧边栏桌面端固定在视口左侧，页面滚动时不移动；窄屏端转为固定在顶部的横向导航。
- 侧边栏左下角新增设置入口，首个设置子页面为更新日志，沿用 `CHANGELOG.md` 的已发布版本摘要。

验收：新增设置与更新日志回归，覆盖 1440px／390px／320px 的版本号上下布局、侧边栏内设置入口、子页面返回、滚动时导航固定、无横向溢出与键盘操作；核对日志与 CHANGELOG 一致。既有分类明细、图表选月及范围回归全部通过，桌面与移动滚动截图已检查。测试使用合成或空账本，无私人数据提交。

位置修正：用户明确设置入口应位于左下角的侧边栏内部，而非页面右下角。已移动到侧栏底部；手机端随侧栏布局放在顶部导航内。设置与图表回归在 1440px／390px／320px 下通过。
