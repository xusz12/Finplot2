# 更新记录

## v0.1.7 - 2026-09-23

- 产品名称下方显示当前版本号 `v0.1.7`。
- 侧边栏固定在视口中，页面上下滚动时保持导航与设置入口可见；窄屏端导航固定在顶部。
- 在侧边栏左下角新增设置入口，首个子页面为更新日志，展示各版本的主要变化。

## v0.1.6 - 2026-09-23

- 总览与投资图表支持点击月份切换到月度数据，再次点击当前月份返回年度数据；主图与副图的月份参考虚线实时同步。
- 保留桌面端柱形悬浮预览，移动端点击图表后同步日期选择状态，并将选中态边框调整为轻量描边。
- 移动端顶栏保持单行布局，导航内容过多时支持横向滚动，避免元素向下堆叠。
- 更新投资页摘要卡片、图表、状态、明细和空状态文案，统一使用盈利、亏损与盈亏平衡等投资语义。
- 增加图表月份选择回归测试，覆盖桌面端、移动端和窄屏布局。

## v0.1.5 - 2026-09-23

- 新增月度收支日历，支持每日收支、当天交易明细、键盘日期导航和移动端布局。
- 总览、收支分析、收支日历新增统计范围悬浮面板：全部与日常、投资、往来、调整互斥，四种性质支持自由多选，不增加“自定义”入口。
- 各页面独立记忆统计范围，刷新后恢复；指标、图表、分类、日历与明细统一使用所选范围。
- 全部交易展示所选期间的所有性质记录，移除收入、支出与收支差额统计框；投资页保持固定投资范围。
- 修复收入构成条形点击后分类明细被错误按支出过滤的问题，分类钻取方向与总览方向状态分离。
- 新增范围与分类明细回归测试，覆盖组合金额、周期、独立记忆、键盘交互及 1440px／390px 布局。

## v0.1.4 - 2026-09-23

- 投资趋势改为平滑的累计收益曲线，保留月度收支差额柱形。
- 投资盈利柱形使用收入红色，亏损柱形使用支出绿色斜纹，零值不绘制可见柱形。
- 财务总览增加与主趋势共享月份坐标的结余柱状副图，并修正响应式对齐。
- 所有图表的月份整格支持悬停数据浮层，默认隐藏曲线节点，悬停时显示参考线。
- 收支分析移除本期观察和收入／支出切换器，改为收入与支出并列模块。


## v0.1.3 - 2026-09-22

- 以紧凑的月度／年度切换、中文日期入口和左右翻页替换原生月份输入框；点击日期展开月份或年份面板。
- 支持回到本月／今年、页面间保留周期，以及键盘导航、Escape 关闭和焦点返回。
- 按全账本首末交易日期限制时间选择；到达边界时箭头与越界日期变灰并禁用，兼容单月与空账本。
- 趋势图标题明确标注所选年份。
- 修复弹窗翻年意外关闭、悬停覆盖选中背景，以及返回当前周期按钮的圆角和文字居中问题。
- 验证桌面与手机布局、真实金额汇总、跨年切换、边界状态及悬停样式。

## v0.1.2 - 2026-09-22

- 为交易明细行增加按明细分类匹配的轻量符号图标，覆盖当前启用的收支类型。
- 对历史停用类型和未知分类使用分类组或通用符号回退，避免旧记录出现空白图标。

## v0.1.1 - 2026-09-22

- 统一指标、交易金额与图表的收支语义色：收入暖红色、支出绿色。
- 收支差额根据正负显示盈余或超支，零差额与无记录使用中性状态。
- 总览与投资趋势使用统一图例，收入实心、支出斜纹；分类条形图沿用相同编码，避免仅凭颜色识别。
- 增加趋势图读屏数据摘要，放大手机端图表标签。
- 移除隐藏金额功能、指标卡底部说明、侧栏左下角冗余描述和统计性质筛选；保留默认日常口径，投资页固定投资口径。

## v0.1.0 - 2026-09-22

### 新增

- 建立只读 SQLite 账本访问层，金额汇总使用整数分。
- 接入真实交易、分类、分类性质和来源字段。
- 实现财务总览、收支分析、分类明细、全部交易和投资记录页面。
- 支持月份、年度、收入/支出和分类性质筛选。
- 支持金额隐私显示、空数据、错误状态和响应式布局。
- 提供本机及 Tailscale 网络访问入口。
- 增加 macOS 双击启动入口 `启动Finplot.command`。
- 支持通过 `FINPLOT_DATABASE` 或 `data/` 自动发现本机账单数据库。

### 约束

- 私人 SQLite 数据库保留在本地 `data/`，不会提交到 Git。
- 投资页只展示数据库中的投资收支记录；数据库没有持仓、行情和账户表，因此不计算持仓市值、浮动收益或收益率。
- 当前服务需要手动运行 `python3 app/server.py`，尚未配置开机自启。

## v0.1.2 修订

- 将交易分类图标统一为同一套线性 SVG，统一描边、尺寸与基线，避免 Emoji 和系统字体混排造成的视觉差异。

## v0.1.2 色彩修订

- 为分类图标加入按行为大类区分的色系：支出使用冷色，收入使用暖色；同一大类保持同一色相。
